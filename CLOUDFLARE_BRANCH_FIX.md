# Cloudflare Pages 分支设置修改指南

## 🔍 问题说明

从你的截图看到：
- Cloudflare Pages 项目名：**"front"**
- 当前构建分支：**"main"**
- 你的本地分支：**"master"**

## ✅ 解决方案

### 方案一：在 Cloudflare Pages 中修改分支（如果使用 Git 连接）

如果你是通过 Cloudflare Pages 控制台连接 GitHub 仓库的：

1. **进入项目设置**
   - 访问：https://dash.cloudflare.com
   - 点击左侧 "Workers & Pages"
   - 找到项目 "front"
   - 点击项目进入详情页

2. **修改生产分支**
   - 点击顶部 "Settings"（设置）标签
   - 找到 "Builds & deployments"（构建和部署）部分
   - 找到 "Production branch"（生产分支）
   - 点击编辑，将 `main` 改为 `master`
   - 点击 "Save"（保存）

3. **重新部署**
   - 返回 "Deployments"（部署）标签
   - 点击 "Retry deployment"（重试部署）

---

### 方案二：使用 GitHub Actions 部署（推荐，不需要修改分支）

如果你使用 GitHub Actions 自动部署（当前方案），**不需要在 Cloudflare Pages 设置分支**！

**原因：**
- GitHub Actions 会直接上传构建产物到 Cloudflare Pages
- 不依赖 Git 连接，所以不需要设置分支
- 分支由 GitHub Actions 工作流控制

**当前配置：**
- GitHub Actions 已配置支持 `main` 和 `master` 两个分支
- 无论你 push 到哪个分支，都会自动部署

**使用方法：**
```bash
# 推送到 master 分支
git push origin master

# 或推送到 main 分支
git push origin main
```

---

## 🔧 已修复的问题

### 1. 项目名已更新

已将 GitHub Actions 配置中的项目名从 `dazuoye` 改为 `front`，匹配你的 Cloudflare Pages 项目。

### 2. 分支配置

GitHub Actions 工作流已配置支持两个分支：
```yaml
branches:
  - main
  - master
```

---

## 📋 检查清单

- [x] GitHub Actions 项目名已改为 `front`
- [x] GitHub Actions 支持 `main` 和 `master` 分支
- [ ] GitHub Secrets 已设置（`CLOUDFLARE_API_TOKEN` 和 `CLOUDFLARE_ACCOUNT_ID`）
- [ ] 代码已推送到 GitHub

---

## 🚀 下一步操作

### 如果使用 GitHub Actions（推荐）：

1. **提交修改**
   ```bash
   git add .
   git commit -m "修复 Cloudflare Pages 项目名"
   git push origin master
   ```

2. **查看部署状态**
   - 进入 GitHub 仓库 → "Actions" 标签
   - 查看工作流运行状态
   - 部署成功后，访问：`https://front.pages.dev`

### 如果使用 Cloudflare Pages Git 连接：

1. **修改分支设置**（按上面的步骤 1-3）
2. **等待自动重新部署**

---

## ❓ 常见问题

### Q1: 如何知道应该用哪个分支？
**A**: 
- 查看本地分支：`git branch`
- 查看远程分支：`git branch -a`
- 使用你主要开发的分支（通常是 `master` 或 `main`）

### Q2: GitHub Actions 部署失败怎么办？
**A**: 
1. 检查 GitHub Secrets 是否正确设置
2. 检查项目名是否匹配（已改为 `front`）
3. 查看 GitHub Actions 日志中的错误信息

### Q3: 可以同时使用两个分支吗？
**A**: 可以！GitHub Actions 配置已支持 `main` 和 `master`，两个分支都会自动部署。

---

## 📊 推荐方案对比

| 方案 | 优点 | 缺点 |
|------|------|------|
| **GitHub Actions** | ✅ 不需要在 Cloudflare 操作<br>✅ 自动部署<br>✅ 详细日志 | 需要配置 Secrets |
| **Cloudflare Git 连接** | ✅ 简单直观 | ❌ 需要网络连接 GitHub<br>❌ 可能白屏 |

**推荐：使用 GitHub Actions**（当前方案）




